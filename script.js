// This is a simple JavaScript script template.

// Define the function that will be executed when the page loads.
function myFunction() {
  // Do something when the page loads.
}

// Add the function to the window.onload event.
window.onload = myFunction;
